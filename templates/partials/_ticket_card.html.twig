<div class="card">
	<div class="card-header p-0">
		<img src="{{ vich_uploader_asset(pass, 'imageFile') }}" alt="{{ pass.name }}" class="img-fluid qc7n4">
	</div>
	<div class="card-body position-relative pt-5">
		<div class="udr94d">
			<div class="zsx8jc fs-1 fw-bold">{{ pass.name }}</div>
		</div>
		<h5 class="card-title">
			{{ pass.price | amount }}
		</h5>
		<span class="badge bg-info">{{ pass.category.name }}</span>
		<div class="m1drx3">
			<p class="card-text">
				{{ pass.content | raw }}
			</p>
		</div>
		<a href="#" class="btn btn-primary btn-sm">Détails</a>
		<a href="#" class="btn btn-success btn-sm">Ajouter</a>
	</div>
</div>
